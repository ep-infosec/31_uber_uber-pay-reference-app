extends components/layout

block scripts
    script(src="/resources/js/script.js")

block header
    include components/header_big

block content
    .block.large.dark
    .container.offset-up
        .form
            .columns.centered
                .column
                    p(style="font-weight: 100;") 
                        | Thank you for topping up!
                    br
                    h3 Response
                    if data != null
                        pre POST https://api-sandbox.uber.com/v1/payments/deposits/#{data.deposit.id}/confirm
                        pre.code=JSON.stringify(data, null, 2)